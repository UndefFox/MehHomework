#!/usr/bin/env Rscript

#Задача 2 
#Создайте три переменных с целыми числами. Проверьте :Сумма первых двух чисел строго больше, чем третье число? Сохраните результат в отдельную переменную

x <- 12;
y <- 10;
z <- 6;

result <- (x + y) > z;